{
  "name": "opencode-claude-bridge",
  "version": "0.1.0",
  "type": "module",
  "description": "OpenCode plugin that bridges to Claude Code via fakechat WebSocket - delegate tasks to Claude Code from within OpenCode",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "src/index.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "prepare": "husky"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "claude-code",
    "bridge",
    "websocket",
    "fakechat",
    "ai-agent"
  ],
  "license": "MIT",
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.0.0"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
